# MCP Services List

🚀 集中管理和自动发布的 MCP 服务仓库

## 特性

- 🚀 **自动发布**：PR 合并到 main 分支后自动发布到 npm
- 📦 **统一管理**：所有 MCP 服务集中在一个仓库
- ✅ **零配置**：开发者无需关心发布流程
- 🔄 **版本控制**：自动检测版本变化，只发布有更新的包
- 💰 **完全免费**：基于 GitHub Actions，无需额外成本

## 快速开始

想要贡献你的 MCP 服务？查看 [CONTRIBUTING.md](CONTRIBUTING.md) 了解详细步骤。

## 可用服务

| 服务名 | 描述 | npm 包 | 版本 |
|--------|------|---------|------|
| _暂无_ | 等待第一个服务... | - | - |

> 💡 成为第一个贡献者！查看下方的贡献指南。

## 工作原理

```
┌─────────────────┐
│ 1. 开发者创建   │
│    MCP 服务     │
│ (packages/xxx)  │
└────────┬────────┘
         │
         v
┌─────────────────┐
│ 2. 更新版本号   │
│ package.json    │
└────────┬────────┘
         │
         v
┌─────────────────┐
│ 3. 提交 PR 并   │
│    合并到 main  │
└────────┬────────┘
         │
         v
┌─────────────────┐
│ 4. GitHub       │
│    Actions 自动 │
│    检测并发布   │
└─────────────────┘
```

### 详细流程

1. **开发者**在 `packages/` 下创建新服务
2. **更新** `package.json` 的版本号
3. **提交** Pull Request
4. **合并后** GitHub Actions 自动：
   - 检测版本变化
   - 运行构建（`npm run build`）
   - 发布到 npm registry
   - 在日志中显示发布结果

## 本地开发

```bash
# 克隆仓库
git clone https://github.com/你的用户名/mcp-list.git
cd mcp-list

# 安装依赖
npm install

# 检查哪些包需要发布（不会真正发布）
npm run check

# 手动发布所有变化的包（需要 npm 登录）
npm run publish:all
```

## 环境要求

- Node.js >= 18
- npm 账号（用于发布）
- GitHub 账号

## 配置说明

### 给维护者：配置 npm Token

1. 登录 [npmjs.com](https://www.npmjs.com)
2. 点击头像 → **Access Tokens**
3. **Generate New Token** → 选择 **Classic Token**
4. 类型选择 **Automation**
5. 复制生成的 token
6. 在 GitHub 仓库：
   - **Settings** → **Secrets and variables** → **Actions**
   - 新建 Secret：
     - Name: `NPM_TOKEN`
     - Value: 粘贴你的 npm token

## 常见问题

### Q: 我的包会被发布到哪里？

A: 发布到公开的 npm registry（npmjs.com），任何人都可以通过 `npx` 安装使用。

### Q: 包名有要求吗？

A: 建议使用 scope 格式：`@your-username/package-name`，避免命名冲突。

### Q: 如何更新已发布的包？

A: 修改代码后，更新版本号（`npm version patch/minor/major`），然后提交 PR。

### Q: 发布失败了怎么办？

A: 查看 GitHub Actions 的日志，常见原因：
- 版本号未更新
- 构建失败（`npm run build`）
- npm token 配置错误
- 包名已被占用

### Q: 可以撤回发布吗？

A: 24 小时内可以使用 `npm unpublish package@version --force` 删除。

## 贡献统计

![Contributors](https://img.shields.io/github/contributors/你的用户名/mcp-list)
![Last Commit](https://img.shields.io/github/last-commit/你的用户名/mcp-list)
![GitHub Stars](https://img.shields.io/github/stars/你的用户名/mcp-list)

## 相关资源

- 📘 [MCP 官方文档](https://modelcontextprotocol.io)
- 🛠️ [node-mcp-template](../node-mcp-template) - AI 辅助 MCP 开发模板
- 📖 [MCP开发入门与实战](../MCP开发入门与实战.md)

## 许可证

MIT License - 详见 [LICENSE](LICENSE)

---

**让 MCP 开发更简单！** 🎉

有问题？[提交 Issue](https://github.com/你的用户名/mcp-list/issues)
