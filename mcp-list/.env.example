# 这个文件是示例，实际上大多数包不需要环境变量
# 如果某个包需要配置（如 ReadMysql 需要数据库），
# 请在该包的目录下创建 .env 文件

# 示例：ReadMysql 可能需要的配置（在 packages/ReadMysql/.env 中）
# DB_HOST=your-mysql-host
# DB_PORT=3306
# DB_USER=your-db-user
# DB_PASSWORD=your-db-password

# 说明：
# 1. 我们不提供统一的环境变量配置
# 2. 每个包如果需要配置，请在包目录下自己创建 .env 文件
# 3. 这样可以保持包的独立性和可移植性
