# 快速开始指南

## 📂 项目结构

```
ai-mcp-study/
├── MCP开发入门与实战.md          # 📚 主文档（从这里开始）
├── 快速开始.md                   # ⚡ 本文件
├── ccReport/                     # 📊 原项目（数据源）
├── node-mcp-demo/                # 🟢 Node.js Demo
│   ├── src/
│   │   ├── index.ts             # MCP服务器入口
│   │   ├── tools.ts             # 8个工具函数
│   │   └── utils/               # 工具模块
│   ├── package.json
│   ├── tsconfig.json
│   ├── Dockerfile
│   ├── docker-compose.yml
│   ├── ecosystem.config.js      # PM2配置
│   ├── deploy-node.sh           # 一键部署脚本
│   └── README.md                # 详细文档
└── python-mcp-demo/              # 🐍 Python Demo
    ├── server.py                 # MCP服务器+8个工具
    ├── utils/                    # 工具模块
    ├── requirements.txt
    ├── Dockerfile
    ├── docker-compose.yml
    ├── supervisor.conf
    ├── deploy-python.sh          # 一键部署脚本
    └── README.md                 # 详细文档
```

## 🚀 5分钟快速体验

### 选择1: Node.js版本（推荐本地使用）

```bash
# 1. 进入目录
cd node-mcp-demo

# 2. 安装依赖
npm install

# 3. 确保配置文件存在
# 方式A: 复制
cp ../ccReport/config/keys.json ./config/keys.json

# 方式B: 创建链接
ln -s ../ccReport/config/keys.json ./config/keys.json

# 4. 启动服务
npm run dev
```

**在Cursor中配置**（macOS/Linux: `~/.config/cursor/config.json`）：

```json
{
  "mcpServers": {
    "claude-stats": {
      "command": "npx",
      "args": ["tsx", "/完整路径/node-mcp-demo/src/index.ts"],
      "env": {
        "KEYS_CONFIG_PATH": "/完整路径/ccReport/config/keys.json"
      }
    }
  }
}
```

### 选择2: Python版本（推荐Docker）

```bash
# 1. 进入目录
cd python-mcp-demo

# 2. Docker一键启动（推荐）
docker-compose up -d

# 查看日志
docker-compose logs -f

# 服务地址: http://localhost:8000/mcp
```

**在Cursor中配置**：

```json
{
  "mcpServers": {
    "claude-stats": {
      "url": "http://localhost:8000/mcp"
    }
  }
}
```

## ✅ 验证安装

重启Cursor后，在对话框中尝试：

```
你：今天使用率最高的是谁？
AI：[会自动调用 query_top_users 工具]
```

## 📖 学习路径

1. **阅读主文档** → [`MCP开发入门与实战.md`](./MCP开发入门与实战.md)
   - 了解MCP是什么
   - 学习基本概念
   - 查看框架对比

2. **体验Demo**
   - Node.js → [`node-mcp-demo/README.md`](./node-mcp-demo/README.md)
   - Python → [`python-mcp-demo/README.md`](./python-mcp-demo/README.md)

3. **选择部署方式**
   - 本地开发：Node.js（通过npm/npx）
   - 远程部署：Python（Docker）或Node.js（PM2）

## 🛠️ 8个可用工具

| 工具名称 | 功能 | 示例问题 |
|---------|------|---------|
| `query_today_stats` | 查询今日统计 | "今天总共花了多少钱？" |
| `query_monthly_stats` | 查询本月统计 | "本月使用情况怎么样？" |
| `query_user_stats` | 查询特定用户 | "查询江俊锋的使用情况" |
| `query_top_users` | Top用户排行 | "使用率最高的是谁？" |
| `compare_users` | 对比用户 | "对比江俊锋和陈雷" |
| `get_usage_trend` | 趋势分析 | "分析使用趋势" |
| `detect_anomalies` | 异常检测 | "有没有超出限额的？" |
| `generate_report` | 生成报告 | "生成今日报告" |

## 🔧 故障排查

### Node.js

```bash
# 检查Node版本（需要 >= 18）
node -v

# 清理重装
rm -rf node_modules package-lock.json
npm install

# 测试TypeScript
npx tsx src/index.ts
```

### Python

```bash
# 检查Python版本（需要 >= 3.8）
python3 --version

# Docker重新构建
docker-compose down
docker-compose build --no-cache
docker-compose up -d

# 本地运行
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python server.py
```

### Cursor配置

1. 确保配置文件路径使用**绝对路径**
2. 重启Cursor
3. 打开开发者工具查看错误日志（Help → Toggle Developer Tools）

## 📚 进一步学习

- [MCP官方文档](https://modelcontextprotocol.io)
- [FastMCP TypeScript](https://github.com/punkpeye/fastmcp)
- [FastMCP Python](https://github.com/jlowin/fastmcp)
- [Cursor MCP文档](https://docs.cursor.com/advanced/mcp)

## 🎯 下一步

- ✅ 体验基本功能
- ✅ 阅读代码了解实现
- ✅ 尝试修改添加新工具
- ✅ 部署到生产环境

---

**有问题？** 查看各Demo的README或主文档！

**祝开发愉快！** 🚀

